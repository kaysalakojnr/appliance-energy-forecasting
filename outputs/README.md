# Outputs

This directory contains the headline figures and metric tables used in the report. Running `python scripts/run_pipeline.py` regenerates `all_forecasts.csv`, `model_comparison.csv`, and the pipeline comparison figures. Raw/intermediate data and model objects are deliberately not committed.
